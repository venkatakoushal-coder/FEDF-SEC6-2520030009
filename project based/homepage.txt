<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>SkyBook – Find & Book Flights</title>
  <link href="https://fonts.googleapis.com/css2?family=Sora:wght@300;400;600;700;800&family=DM+Sans:wght@300;400;500&display=swap" rel="stylesheet"/>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --sky: #0ea5e9;
      --deep: #0369a1;
      --night: #0c1a2e;
      --card: #111e30;
      --glass: rgba(255,255,255,0.06);
      --border: rgba(255,255,255,0.1);
      --text: #e2eaf4;
      --muted: #7a97b5;
      --accent: #38bdf8;
      --success: #34d399;
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'DM Sans', sans-serif;
      background: var(--night);
      color: var(--text);
      min-height: 100vh;
      overflow-x: hidden;
    }

    /* ── BG ── */
    .bg-layer {
      position: fixed; inset: 0; z-index: 0; pointer-events: none;
      background:
        radial-gradient(ellipse 80% 60% at 70% 10%, rgba(14,165,233,0.15) 0%, transparent 60%),
        radial-gradient(ellipse 50% 40% at 10% 80%, rgba(56,189,248,0.08) 0%, transparent 50%),
        linear-gradient(160deg, #0c1a2e 0%, #071120 100%);
    }
    .stars {
      position: fixed; inset: 0; z-index: 0; pointer-events: none;
      background-image:
        radial-gradient(1px 1px at 20% 30%, rgba(255,255,255,0.6) 0%, transparent 100%),
        radial-gradient(1px 1px at 80% 10%, rgba(255,255,255,0.5) 0%, transparent 100%),
        radial-gradient(1px 1px at 50% 60%, rgba(255,255,255,0.4) 0%, transparent 100%),
        radial-gradient(1px 1px at 30% 80%, rgba(255,255,255,0.3) 0%, transparent 100%),
        radial-gradient(1px 1px at 90% 50%, rgba(255,255,255,0.5) 0%, transparent 100%),
        radial-gradient(1px 1px at 60% 20%, rgba(255,255,255,0.6) 0%, transparent 100%),
        radial-gradient(1.5px 1.5px at 15% 55%, rgba(255,255,255,0.4) 0%, transparent 100%),
        radial-gradient(1px 1px at 75% 75%, rgba(255,255,255,0.3) 0%, transparent 100%);
    }

    /* ── NAV ── */
    nav {
      position: fixed; top: 0; width: 100%; z-index: 100;
      display: flex; align-items: center; justify-content: space-between;
      padding: 18px 48px;
      background: rgba(12,26,46,0.8);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      animation: slideDown 0.6s ease;
    }
    @keyframes slideDown { from { transform: translateY(-100%); opacity: 0; } to { transform: translateY(0); opacity: 1; } }

    .logo {
      font-family: 'Sora', sans-serif;
      font-weight: 800; font-size: 1.5rem;
      color: #fff; letter-spacing: -0.5px;
    }
    .logo span { color: var(--accent); }

    .nav-links { display: flex; gap: 32px; list-style: none; }
    .nav-links a {
      color: var(--muted); text-decoration: none;
      font-size: 0.9rem; font-weight: 500;
      transition: color 0.2s;
    }
    .nav-links a:hover { color: #fff; }

    .nav-btns { display: flex; gap: 12px; }
    .btn-outline {
      padding: 8px 20px; border-radius: 8px;
      border: 1px solid var(--border); background: transparent;
      color: var(--text); font-size: 0.88rem; font-family: 'DM Sans', sans-serif;
      cursor: pointer; transition: all 0.2s;
    }
    .btn-outline:hover { border-color: var(--accent); color: var(--accent); }

    .btn-primary {
      padding: 8px 20px; border-radius: 8px;
      border: none; background: var(--sky);
      color: #fff; font-size: 0.88rem; font-weight: 600;
      font-family: 'DM Sans', sans-serif;
      cursor: pointer; transition: all 0.2s;
    }
    .btn-primary:hover { background: var(--deep); transform: translateY(-1px); }

    /* ── HERO ── */
    .hero {
      position: relative; z-index: 1;
      min-height: 100vh;
      display: flex; flex-direction: column;
      align-items: center; justify-content: center;
      padding: 120px 24px 60px;
      text-align: center;
    }

    .badge {
      display: inline-flex; align-items: center; gap: 6px;
      background: rgba(14,165,233,0.12); border: 1px solid rgba(14,165,233,0.3);
      color: var(--accent); padding: 6px 14px; border-radius: 100px;
      font-size: 0.8rem; font-weight: 500; margin-bottom: 24px;
      animation: fadeUp 0.7s ease 0.2s both;
    }
    .badge::before { content: "✈"; font-size: 0.85rem; }

    .hero h1 {
      font-family: 'Sora', sans-serif;
      font-size: clamp(2.4rem, 6vw, 4.2rem);
      font-weight: 800; line-height: 1.1;
      letter-spacing: -1.5px; margin-bottom: 20px;
      animation: fadeUp 0.7s ease 0.3s both;
    }
    .hero h1 .highlight {
      background: linear-gradient(90deg, var(--accent), #60d4fa);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    }

    .hero p {
      font-size: 1.05rem; color: var(--muted);
      max-width: 500px; line-height: 1.7;
      margin-bottom: 40px;
      animation: fadeUp 0.7s ease 0.4s both;
    }

    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    /* ── SEARCH BAR ── */
    .search-box {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 16px;
      padding: 24px 28px;
      width: 100%; max-width: 780px;
      display: flex; flex-wrap: wrap; gap: 12px; align-items: flex-end;
      animation: fadeUp 0.7s ease 0.5s both;
      box-shadow: 0 24px 60px rgba(0,0,0,0.4);
    }

    .field-group { display: flex; flex-direction: column; gap: 6px; flex: 1; min-width: 130px; }
    .field-group label { font-size: 0.75rem; color: var(--muted); font-weight: 500; text-transform: uppercase; letter-spacing: 0.5px; }
    .field-group input, .field-group select {
      background: rgba(255,255,255,0.05);
      border: 1px solid var(--border);
      border-radius: 8px; padding: 10px 14px;
      color: var(--text); font-size: 0.9rem;
      font-family: 'DM Sans', sans-serif;
      outline: none; transition: border-color 0.2s;
      width: 100%;
    }
    .field-group input::placeholder { color: #3d5a75; }
    .field-group input:focus, .field-group select:focus { border-color: var(--sky); }
    .field-group select option { background: var(--card); color: var(--text); }

    .search-btn {
      padding: 11px 28px; border-radius: 8px;
      border: none; background: var(--sky);
      color: #fff; font-size: 0.95rem; font-weight: 600;
      font-family: 'DM Sans', sans-serif;
      cursor: pointer; transition: all 0.2s;
      white-space: nowrap;
    }
    .search-btn:hover { background: var(--deep); transform: translateY(-2px); box-shadow: 0 8px 20px rgba(14,165,233,0.3); }

    /* ── STATS ── */
    .stats {
      display: flex; gap: 40px; margin-top: 40px; flex-wrap: wrap; justify-content: center;
      animation: fadeUp 0.7s ease 0.6s both;
    }
    .stat { text-align: center; }
    .stat .num { font-family: 'Sora', sans-serif; font-size: 1.6rem; font-weight: 700; color: #fff; }
    .stat .lbl { font-size: 0.8rem; color: var(--muted); margin-top: 2px; }

    /* ── AUTH MODAL ── */
    .overlay {
      position: fixed; inset: 0; z-index: 200;
      background: rgba(7,17,32,0.85);
      backdrop-filter: blur(8px);
      display: none; align-items: center; justify-content: center;
      padding: 20px;
    }
    .overlay.active { display: flex; }

    .modal {
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 20px;
      padding: 40px 36px;
      width: 100%; max-width: 420px;
      position: relative;
      animation: popIn 0.3s cubic-bezier(0.34,1.56,0.64,1);
    }
    @keyframes popIn {
      from { opacity: 0; transform: scale(0.88); }
      to   { opacity: 1; transform: scale(1); }
    }

    .modal-close {
      position: absolute; top: 16px; right: 20px;
      background: none; border: none; color: var(--muted);
      font-size: 1.4rem; cursor: pointer; line-height: 1;
      transition: color 0.2s;
    }
    .modal-close:hover { color: #fff; }

    .modal-logo { font-family: 'Sora', sans-serif; font-size: 1.3rem; font-weight: 800; margin-bottom: 6px; }
    .modal-logo span { color: var(--accent); }

    .tab-row {
      display: flex; gap: 0; margin: 20px 0 28px;
      background: rgba(255,255,255,0.05);
      border-radius: 10px; padding: 4px;
    }
    .tab {
      flex: 1; padding: 9px; border-radius: 8px;
      border: none; background: none; color: var(--muted);
      font-size: 0.9rem; font-weight: 500;
      font-family: 'DM Sans', sans-serif;
      cursor: pointer; transition: all 0.2s;
    }
    .tab.active { background: var(--sky); color: #fff; }

    .form-field { margin-bottom: 16px; }
    .form-field label { display: block; font-size: 0.8rem; color: var(--muted); margin-bottom: 6px; font-weight: 500; }
    .form-field input {
      width: 100%; padding: 12px 16px;
      background: rgba(255,255,255,0.05);
      border: 1px solid var(--border); border-radius: 10px;
      color: var(--text); font-size: 0.92rem;
      font-family: 'DM Sans', sans-serif; outline: none;
      transition: border-color 0.2s;
    }
    .form-field input:focus { border-color: var(--sky); background: rgba(14,165,233,0.06); }
    .form-field input::placeholder { color: #3d5a75; }

    .form-submit {
      width: 100%; padding: 13px;
      background: var(--sky); border: none; border-radius: 10px;
      color: #fff; font-size: 0.95rem; font-weight: 600;
      font-family: 'DM Sans', sans-serif;
      cursor: pointer; margin-top: 8px;
      transition: all 0.2s;
    }
    .form-submit:hover { background: var(--deep); transform: translateY(-1px); box-shadow: 0 8px 20px rgba(14,165,233,0.25); }

    .form-footer { text-align: center; margin-top: 16px; font-size: 0.82rem; color: var(--muted); }
    .form-footer a { color: var(--accent); text-decoration: none; }

    .success-msg {
      display: none; text-align: center; padding: 16px;
      background: rgba(52,211,153,0.1); border: 1px solid rgba(52,211,153,0.3);
      border-radius: 10px; color: var(--success);
      font-size: 0.9rem; margin-top: 12px;
    }

    /* ── FEATURES ── */
    .features {
      position: relative; z-index: 1;
      padding: 80px 48px;
      max-width: 1100px; margin: 0 auto;
    }
    .section-label {
      font-size: 0.78rem; text-transform: uppercase; letter-spacing: 2px;
      color: var(--accent); font-weight: 600; margin-bottom: 12px;
    }
    .section-title {
      font-family: 'Sora', sans-serif;
      font-size: clamp(1.6rem, 3vw, 2.4rem);
      font-weight: 700; color: #fff; margin-bottom: 48px;
    }

    .cards { display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); gap: 20px; }
    .card {
      background: var(--glass);
      border: 1px solid var(--border);
      border-radius: 16px; padding: 28px 24px;
      transition: transform 0.2s, border-color 0.2s;
    }
    .card:hover { transform: translateY(-4px); border-color: rgba(14,165,233,0.4); }
    .card-icon { font-size: 1.8rem; margin-bottom: 14px; }
    .card h3 { font-family: 'Sora', sans-serif; font-size: 1rem; font-weight: 600; margin-bottom: 8px; color: #fff; }
    .card p { font-size: 0.87rem; color: var(--muted); line-height: 1.6; }

    /* ── FOOTER ── */
    footer {
      position: relative; z-index: 1;
      text-align: center; padding: 32px;
      border-top: 1px solid var(--border);
      color: var(--muted); font-size: 0.82rem;
    }
    footer span { color: var(--accent); }

    @media (max-width: 640px) {
      nav { padding: 16px 20px; }
      .nav-links { display: none; }
      .search-box { padding: 18px; }
      .features { padding: 60px 20px; }
    }
  </style>
</head>
<body>

<div class="bg-layer"></div>
<div class="stars"></div>

<!-- NAV -->
<nav>
  <div class="logo">Sky<span>Book</span></div>
  <ul class="nav-links">
    <li><a href="#">Home</a></li>
    <li><a href="#">Flights</a></li>
    <li><a href="#">My Bookings</a></li>
    <li><a href="#">Support</a></li>
  </ul>
  <div class="nav-btns">
    <button class="btn-outline" onclick="openModal('login')">Log In</button>
    <button class="btn-primary" onclick="openModal('signup')">Sign Up</button>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="badge">Your smart flight companion</div>
  <h1>Book Flights<br>with <span class="highlight">Zero Hassle</span></h1>
  <p>Search hundreds of airlines, compare prices, pick your seat, and get your e-ticket — all in one place.</p>

  <div class="search-box">
    <div class="field-group">
      <label>From</label>
      <input type="text" placeholder="e.g. Hyderabad" />
    </div>
    <div class="field-group">
      <label>To</label>
      <input type="text" placeholder="e.g. Delhi" />
    </div>
    <div class="field-group">
      <label>Date</label>
      <input type="date" />
    </div>
    <div class="field-group">
      <label>Class</label>
      <select>
        <option>Economy</option>
        <option>Business</option>
        <option>First Class</option>
      </select>
    </div>
    <button class="search-btn">Search ✈</button>
  </div>

  <div class="stats">
    <div class="stat"><div class="num">200+</div><div class="lbl">Airlines</div></div>
    <div class="stat"><div class="num">500+</div><div class="lbl">Destinations</div></div>
    <div class="stat"><div class="num">50K+</div><div class="lbl">Bookings Done</div></div>
    <div class="stat"><div class="num">4.8★</div><div class="lbl">User Rating</div></div>
  </div>
</section>

<!-- FEATURES -->
<section class="features">
  <div class="section-label">Why SkyBook</div>
  <div class="section-title">Everything you need to fly smarter</div>
  <div class="cards">
    <div class="card">
      <div class="card-icon">🔍</div>
      <h3>Smart Flight Search</h3>
      <p>Search by source, destination, date, and class with real-time results and smart filters.</p>
    </div>
    <div class="card">
      <div class="card-icon">💺</div>
      <h3>Seat Selection</h3>
      <p>Pick your preferred seat on an interactive seat map before confirming your booking.</p>
    </div>
    <div class="card">
      <div class="card-icon">🎫</div>
      <h3>Instant E-Ticket</h3>
      <p>Get a downloadable e-ticket with PNR and all flight details right after payment.</p>
    </div>
    <div class="card">
      <div class="card-icon">📋</div>
      <h3>Booking History</h3>
      <p>View and manage all your past and upcoming bookings from your personal dashboard.</p>
    </div>
  </div>
</section>

<!-- FOOTER -->
<footer>
  © 2025 <span>SkyBook</span> · Built for 25CS1201E · Front End Development Frameworks
</footer>

<!-- AUTH MODAL -->
<div class="overlay" id="authOverlay" onclick="closeOnOverlay(event)">
  <div class="modal" id="authModal">
    <button class="modal-close" onclick="closeModal()">×</button>
    <div class="modal-logo">Sky<span>Book</span></div>
    <p style="color:var(--muted);font-size:0.85rem;">Welcome! Please login or create an account.</p>

    <div class="tab-row">
      <button class="tab active" id="tabLogin" onclick="switchTab('login')">Log In</button>
      <button class="tab" id="tabSignup" onclick="switchTab('signup')">Sign Up</button>
    </div>

    <!-- Login Form -->
    <div id="loginForm">
      <div class="form-field">
        <label>Username</label>
        <input type="text" id="loginUser" placeholder="Enter your username" />
      </div>
      <div class="form-field">
        <label>Password</label>
        <input type="password" id="loginPass" placeholder="Enter your password" />
      </div>
      <button class="form-submit" onclick="handleLogin()">Log In</button>
      <div class="success-msg" id="loginSuccess">✅ Logged in successfully! Redirecting...</div>
      <div class="form-footer">Don't have an account? <a href="#" onclick="switchTab('signup')">Sign up</a></div>
    </div>

    <!-- Signup Form -->
    <div id="signupForm" style="display:none;">
      <div class="form-field">
        <label>Full Name</label>
        <input type="text" id="signupName" placeholder="Enter your full name" />
      </div>
      <div class="form-field">
        <label>Username</label>
        <input type="text" id="signupUser" placeholder="Choose a username" />
      </div>
      <div class="form-field">
        <label>Password</label>
        <input type="password" id="signupPass" placeholder="Create a password" />
      </div>
      <button class="form-submit" onclick="handleSignup()">Create Account</button>
      <div class="success-msg" id="signupSuccess">✅ Account created! You can now log in.</div>
      <div class="form-footer">Already have an account? <a href="#" onclick="switchTab('login')">Log in</a></div>
    </div>
  </div>
</div>

<script>
  function openModal(tab) {
    document.getElementById('authOverlay').classList.add('active');
    switchTab(tab);
  }
  function closeModal() {
    document.getElementById('authOverlay').classList.remove('active');
  }
  function closeOnOverlay(e) {
    if (e.target.id === 'authOverlay') closeModal();
  }
  function switchTab(tab) {
    const isLogin = tab === 'login';
    document.getElementById('loginForm').style.display = isLogin ? 'block' : 'none';
    document.getElementById('signupForm').style.display = isLogin ? 'none' : 'block';
    document.getElementById('tabLogin').classList.toggle('active', isLogin);
    document.getElementById('tabSignup').classList.toggle('active', !isLogin);
    document.getElementById('loginSuccess').style.display = 'none';
    document.getElementById('signupSuccess').style.display = 'none';
  }
  function handleLogin() {
    const u = document.getElementById('loginUser').value.trim();
    const p = document.getElementById('loginPass').value.trim();
    if (!u || !p) { alert('Please fill in all fields.'); return; }
    document.getElementById('loginSuccess').style.display = 'block';
    setTimeout(closeModal, 2000);
  }
  function handleSignup() {
    const n = document.getElementById('signupName').value.trim();
    const u = document.getElementById('signupUser').value.trim();
    const p = document.getElementById('signupPass').value.trim();
    if (!n || !u || !p) { alert('Please fill in all fields.'); return; }
    document.getElementById('signupSuccess').style.display = 'block';
    setTimeout(() => switchTab('login'), 2000);
  }
</script>
</body>
</html>
